<%= provide(:title, 'Je naam') %>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<div class="name-selection" tabindex="1">
			<p>Vul je naam in.</p>
			<span class="cyclic_input active l1" tabindex="0">A</span>
			<span class="cyclic_input l2" tabindex="0">A</span>
			<span class="cyclic_input l3" tabindex="0">A</span>
			<%= form_for(@user) do |f| %>
				<%= render 'shared/error_messages' %>
					<%= f.hidden_field :name %>
				<%= f.submit "Let's Build!", class: "cyclic_input btn btn-default btn-lg pull-right" %>
			<% end %>
		</div>
	</div>
</div>
